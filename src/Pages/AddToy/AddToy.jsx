import useDynamicTitle from "../../useDynamicTitle/useDynamicTitle";

const AddToy = () => {
  useDynamicTitle("Add-Toy");
  return (
    <div className="my-20">
      <h2 className="mt-10 text-3xl text-center font-bold">Add A Toy</h2>
    </div>
  );
};

export default AddToy;
